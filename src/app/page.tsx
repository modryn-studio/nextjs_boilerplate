// TODO /setup: replace this stub entirely with the project's home page
// Built from context.md (hero copy, CTA) and brand.md (colors, fonts, voice)
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-bg">
      <p className="text-muted font-mono text-sm">Run /setup to initialize this project.</p>
    </main>
  );
}
