# ── Analytics ─────────────────────────────────────────────
# Vercel Analytics is zero-config — no env vars needed.
# Enable it in the Vercel dashboard: Analytics tab → Enable.

# ── Admin ────────────────────────────────────────────
# Password for the /admin dashboard (sent as Authorization: Bearer <pw>)
ADMIN_PASSWORD=your_admin_password_here

# ── Email (nodemailer → Gmail SMTP) ──────────────────
GMAIL_USER=you@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx   # App password, NOT your Gmail password
FEEDBACK_TO=you@gmail.com                # Defaults to GMAIL_USER if blank

# ── Resend (contacts + transactional emails) ─────────
# Verify one sending domain in Resend (e.g. modrynstudio.com) — reuse it across all projects.
# Use a display name to carry the product brand: "Product Name <hello@yourdomain.com>"
#
# Contacts are tagged with source=site.name so all projects are filterable
# in the shared Resend account audience without needing separate segments.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=Product Name <hello@yourdomain.com>
RESEND_SEGMENT_ID=                              # Optional — tags signups to a named Resend segment (create at resend.com → Audience → Segments)

# ── Site URL ──────────────────────────────────────────────
# Must match Vercel deployment URL in production.
# Also used by Neon Auth's withCanonicalOrigin() wrapper — must be set in Vercel env vars too.
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# ── Neon Auth (@neondatabase/auth) ────────────────────
# Only needed if using @neondatabase/auth in this project.
# Get these from the Neon console: your project → Auth tab.
#
# NEON_AUTH_BASE_URL — the full endpoint URL for your Neon Auth project:
#   https://<endpoint-id>.neonauth.<region>.aws.neon.tech/<db>/auth
# NEON_AUTH_COOKIE_SECRET — any random 32+ char string. Generate one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Also required after setup — update trusted_origins in the DB:
#   UPDATE neon_auth.project_config
#   SET trusted_origins = '["https://yourdomain.com", "https://www.yourdomain.com"]'::jsonb
#   WHERE project = 'your-project-name';
#
NEON_AUTH_BASE_URL=https://<endpoint-id>.neonauth.<region>.aws.neon.tech/<db>/auth
NEON_AUTH_COOKIE_SECRET=your_32_plus_char_secret_here

# ── Stripe (one-time payments) ────────────────────────
# Two approaches — pick one:
#
# PAYMENT LINKS (recommended — no env vars needed here):
#   Create a Payment Link at stripe.com → Payment Links.
#   Set success_url to your tool page with ?paid=true appended.
#   Pass the link URL as the `checkoutUrl` prop on <PayGate>.
#   No server code, no env vars, no npm package needed.
#
# CHECKOUT SESSIONS (upgrade path — needs these env vars):
#   Use when you need dynamic pricing, coupons, or programmatic control.
#   Requires `npm install stripe` and the /api/checkout route.
#
# TEST MODE WORKFLOW:
#   1. Use test keys during development (stripe.com → Developers → API keys → Test mode)
#   2. Create a test product + price in test mode
#   3. Test the full checkout flow with card 4242 4242 4242 4242
#   4. Switch to live keys + live price ID at deploy time
#   5. IMPORTANT: disable Cash App Pay in Stripe Dashboard → Settings → Payment methods
#      (90%+ of disputed/cancelled transactions come from Cash App Pay)
#
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
STRIPE_PRICE_ID=your_stripe_price_id_here
# Future: webhook verification (not needed for Payment Links or basic Checkout Sessions)
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here
